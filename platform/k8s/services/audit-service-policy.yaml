# Per-service companion objects for audit-service.
#
# Auto-generated by scripts/gen-service-companions.sh.
# Tier: T1
# App port 8083, actuator port 9093.
# PriorityClass: service-tier-1.
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: audit-service
  labels:
    app.kubernetes.io/name: audit-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: audit-service
  minReplicas: 2
  maxReplicas: 8
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 100
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 25
          periodSeconds: 60
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Pods
      pods:
        metric:
          name: audit_consumer_lag_seconds
        target:
          type: AverageValue
          averageValue: "30"
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: audit-service
  labels:
    app.kubernetes.io/name: audit-service
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: audit-service
---
# NetworkPolicy — audit-service allows ingress only from api-gateway +
# admin-service + kubelet probes. Egress is covered by the
# platform-wide allow-services-to-* + allow-* policies.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: audit-service
  labels:
    app.kubernetes.io/name: audit-service
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: audit-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
      ports:
        - protocol: TCP
          port: 8083
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: admin-service
      ports:
        - protocol: TCP
          port: 8083
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: audit-service
  labels:
    app.kubernetes.io/name: audit-service
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: audit-service
  endpoints:
    - port: actuator
      path: /actuator/prometheus
      interval: 15s
      scrapeTimeout: 10s
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: audit-service
  labels:
    app.kubernetes.io/name: audit-service
spec:
  groups:
    - name: audit-service.slo
      interval: 30s
      rules:
        - alert: audit_serviceSLOErrorRateHigh
          expr: |
            (
              sum(rate(http_server_requests_seconds_count{service="audit-service",status=~"5.."}[1h]))
              /
              sum(rate(http_server_requests_seconds_count{service="audit-service"}[1h]))
            ) > 0.005
          for: 5m
          labels:
            severity: warning
            team: platform
          annotations:
            summary: "audit-service error rate > 0.5% (1h)"
            runbook: "https://wiki.trips-enjoy.com/runbooks/audit-service"
        - alert: audit_serviceP99LatencyHigh
          expr: |
            histogram_quantile(0.99,
              sum by (route, le) (rate(http_server_requests_seconds_bucket{service="audit-service"}[5m]))
            ) > 1.0
          for: 5m
          labels:
            severity: warning
            team: platform
          annotations:
            summary: "audit-service p99 latency > 1s"
        - alert: AuditServiceHashChainMismatch
          expr: increase(audit_hash_chain_mismatch_total[5m]) > 0
          for: 1m
          labels:
            severity: critical
            team: security-oncall
          annotations:
            summary: 'audit-service hash chain mismatch (TAMPER ALERT)'
            runbook: 'https://wiki.trips-enjoy.com/runbooks/audit-tamper'
            priority: P0
---
# Helm-managed migration Job. Runs DB migrations before the
# Deployment rolls. Migrate image is the service's own image; the
# entrypoint picks the migrate subcommand.
apiVersion: batch/v1
kind: Job
metadata:
  name: audit-service-migrate
  labels:
    app.kubernetes.io/name: audit-service
    app.kubernetes.io/job: migrate
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade,pre-rollback
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: audit-service
        app.kubernetes.io/job: migrate
    spec:
      restartPolicy: OnFailure
      serviceAccountName: audit-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: migrate
          image: registry.trips-enjoy.com/audit-service:dev-k8s
          imagePullPolicy: IfNotPresent
          args:
            - migrate
            - --spring.main.web-application-type=none
          envFrom:
            - secretRef:
                name: audit-service-runtime
                optional: true
            - configMapRef:
                name: audit-service-config
                optional: true
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",   memory: 1Gi  }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
