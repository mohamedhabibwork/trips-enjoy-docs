# Per-service companion objects for file-service.
#
# Auto-generated by scripts/gen-service-companions.sh.
# Tier: T2
# App port 8101, actuator port 9111.
# PriorityClass: service-tier-2.
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: file-service
  labels:
    app.kubernetes.io/name: file-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: file-service
  minReplicas: 6
  maxReplicas: 12
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 100
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 25
          periodSeconds: 60
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
    - type: Pods
      pods:
        metric:
          name: files_uploads_per_second
        target:
          type: AverageValue
          averageValue: "50"
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: file-service
  labels:
    app.kubernetes.io/name: file-service
spec:
  minAvailable: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: file-service
---
# NetworkPolicy — file-service allows ingress only from api-gateway +
# admin-service + kubelet probes. Egress is covered by the
# platform-wide allow-services-to-* + allow-* policies.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: file-service
  labels:
    app.kubernetes.io/name: file-service
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: file-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
      ports:
        - protocol: TCP
          port: 8101
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: admin-service
      ports:
        - protocol: TCP
          port: 8101
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: file-service
  labels:
    app.kubernetes.io/name: file-service
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: file-service
  endpoints:
    - port: actuator
      path: /actuator/prometheus
      interval: 15s
      scrapeTimeout: 10s
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: file-service
  labels:
    app.kubernetes.io/name: file-service
spec:
  groups:
    - name: file-service.slo
      interval: 30s
      rules:
        - alert: file_serviceSLOErrorRateHigh
          expr: |
            (
              sum(rate(http_server_requests_seconds_count{service="file-service",status=~"5.."}[1h]))
              /
              sum(rate(http_server_requests_seconds_count{service="file-service"}[1h]))
            ) > 0.005
          for: 5m
          labels:
            severity: warning
            team: platform
          annotations:
            summary: "file-service error rate > 0.5% (1h)"
            runbook: "https://wiki.trips-enjoy.com/runbooks/file-service"
        - alert: file_serviceP99LatencyHigh
          expr: |
            histogram_quantile(0.99,
              sum by (route, le) (rate(http_server_requests_seconds_bucket{service="file-service"}[5m]))
            ) > 1.0
          for: 5m
          labels:
            severity: warning
            team: platform
          annotations:
            summary: "file-service p99 latency > 1s"
---
# Helm-managed migration Job. Runs DB migrations before the
# Deployment rolls. Migrate image is the service's own image; the
# entrypoint picks the migrate subcommand.
apiVersion: batch/v1
kind: Job
metadata:
  name: file-service-migrate
  labels:
    app.kubernetes.io/name: file-service
    app.kubernetes.io/job: migrate
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade,pre-rollback
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: file-service
        app.kubernetes.io/job: migrate
    spec:
      restartPolicy: OnFailure
      serviceAccountName: file-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: migrate
          image: registry.trips-enjoy.com/file-service:dev-k8s
          imagePullPolicy: IfNotPresent
          args:
            - migrate
            - up
          envFrom:
            - secretRef:
                name: file-service-runtime
                optional: true
            - configMapRef:
                name: file-service-config
                optional: true
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",   memory: 1Gi  }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
