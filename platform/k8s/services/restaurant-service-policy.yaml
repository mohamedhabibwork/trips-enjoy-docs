# Per-service companion objects for restaurant-service.
#
# Auto-generated by scripts/gen-service-companions.sh.
# Tier: T2
# App port 8094, actuator port 9104.
# PriorityClass: service-tier-2.
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: restaurant-service
  labels:
    app.kubernetes.io/name: restaurant-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: restaurant-service
  minReplicas: 3
  maxReplicas: 12
  behavior:
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
        - type: Percent
          value: 100
          periodSeconds: 60
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
        - type: Percent
          value: 25
          periodSeconds: 60
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: restaurant-service
  labels:
    app.kubernetes.io/name: restaurant-service
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: restaurant-service
---
# NetworkPolicy — restaurant-service allows ingress only from api-gateway +
# admin-service + kubelet probes. Egress is covered by the
# platform-wide allow-services-to-* + allow-* policies.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: restaurant-service
  labels:
    app.kubernetes.io/name: restaurant-service
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: restaurant-service
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: api-gateway
      ports:
        - protocol: TCP
          port: 8094
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: admin-service
      ports:
        - protocol: TCP
          port: 8094
---
apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: restaurant-service
  labels:
    app.kubernetes.io/name: restaurant-service
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: restaurant-service
  endpoints:
    - port: actuator
      path: /actuator/prometheus
      interval: 15s
      scrapeTimeout: 10s
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: restaurant-service
  labels:
    app.kubernetes.io/name: restaurant-service
spec:
  groups:
    - name: restaurant-service.slo
      interval: 30s
      rules:
        - alert: restaurant_serviceSLOErrorRateHigh
          expr: |
            (
              sum(rate(http_server_requests_seconds_count{service="restaurant-service",status=~"5.."}[1h]))
              /
              sum(rate(http_server_requests_seconds_count{service="restaurant-service"}[1h]))
            ) > 0.005
          for: 5m
          labels:
            severity: warning
            team: platform
          annotations:
            summary: "restaurant-service error rate > 0.5% (1h)"
            runbook: "https://wiki.trips-enjoy.com/runbooks/restaurant-service"
        - alert: restaurant_serviceP99LatencyHigh
          expr: |
            histogram_quantile(0.99,
              sum by (route, le) (rate(http_server_requests_seconds_bucket{service="restaurant-service"}[5m]))
            ) > 1.0
          for: 5m
          labels:
            severity: warning
            team: platform
          annotations:
            summary: "restaurant-service p99 latency > 1s"
---
# Helm-managed migration Job. Runs DB migrations before the
# Deployment rolls. Migrate image is the service's own image; the
# entrypoint picks the migrate subcommand.
apiVersion: batch/v1
kind: Job
metadata:
  name: restaurant-service-migrate
  labels:
    app.kubernetes.io/name: restaurant-service
    app.kubernetes.io/job: migrate
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade,pre-rollback
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 2
  ttlSecondsAfterFinished: 600
  template:
    metadata:
      labels:
        app.kubernetes.io/name: restaurant-service
        app.kubernetes.io/job: migrate
    spec:
      restartPolicy: OnFailure
      serviceAccountName: restaurant-service
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
      containers:
        - name: migrate
          image: registry.trips-enjoy.com/restaurant-service:dev-k8s
          imagePullPolicy: IfNotPresent
          args:
            - migrate
            - --spring.main.web-application-type=none
          envFrom:
            - secretRef:
                name: restaurant-service-runtime
                optional: true
            - configMapRef:
                name: restaurant-service-config
                optional: true
          resources:
            requests: { cpu: 250m, memory: 512Mi }
            limits:   { cpu: "1",   memory: 1Gi  }
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
