spring:
  datasource:
    url: ${RESTAURANT_SERVICE_DB_URL:jdbc:postgresql://0.0.0.0:5432/trips_enjoy?currentSchema=restaurant}
    username: ${RESTAURANT_SERVICE_DB_USERNAME:postgres}
    password: ${RESTAURANT_SERVICE_DB_PASSWORD:}
    hikari:
      connection-init-sql: 'CREATE SCHEMA IF NOT EXISTS restaurant;'
  data:
    redis:
      host: ${RESTAURANT_SERVICE_REDIS_HOST:0.0.0.0}
      port: ${RESTAURANT_SERVICE_REDIS_PORT:6379}
      password: ${RESTAURANT_SERVICE_REDIS_PASSWORD:}
  kafka:
    bootstrap-servers: ${RESTAURANT_SERVICE_KAFKA_BOOTSTRAP_SERVERS:http://81.208.166.110:9092}

restaurant-service:
  keycloak:
    base-url: ${RESTAURANT_SERVICE_KEYCLOAK_BASE_URL:http://0.0.0.0:8181}
    issuer-uri: ${RESTAURANT_SERVICE_KEYCLOAK_ISSUER_URI:http://0.0.0.0:8181/realms/platform-services}
    jwks-uri: ${RESTAURANT_SERVICE_KEYCLOAK_JWKS_URI:http://0.0.0.0:8181/realms/platform-services/protocol/openid-connect/certs}
    admin-realm: ${RESTAURANT_SERVICE_KEYCLOAK_ADMIN_REALM:master}
    admin-client-id: ${RESTAURANT_SERVICE_KEYCLOAK_ADMIN_CLIENT_ID:}
    admin-client-secret: ${RESTAURANT_SERVICE_KEYCLOAK_ADMIN_CLIENT_SECRET:}
    admin-username: ${RESTAURANT_SERVICE_KEYCLOAK_ADMIN_USERNAME:}
    admin-password: ${RESTAURANT_SERVICE_KEYCLOAK_ADMIN_PASSWORD:}
