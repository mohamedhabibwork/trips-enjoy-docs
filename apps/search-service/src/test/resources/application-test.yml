spring:
  flyway:
    enabled: true
    schemas: search
    default-schema: search
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: search

search:
  security:
    enabled: false
  outbox:
    poll-interval-ms: 60000