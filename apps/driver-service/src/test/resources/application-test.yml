spring:
  flyway:
    enabled: true
    schemas: driver
    default-schema: driver
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: driver

driver:
  security:
    enabled: false
  outbox:
    poll-interval-ms: 60000