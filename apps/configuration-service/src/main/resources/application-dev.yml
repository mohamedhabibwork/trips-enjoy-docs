spring:
  datasource:
    url: ${CONFIGURATION_SERVICE_DB_URL:jdbc:postgresql://0.0.0.0:5432/trips_enjoy?currentSchema=configuration}
    username: ${CONFIGURATION_SERVICE_DB_USERNAME:postgres}
    password: ${CONFIGURATION_SERVICE_DB_PASSWORD:}
    hikari:
      connection-init-sql: 'CREATE SCHEMA IF NOT EXISTS configuration;'
  data:
    redis:
      host: ${CONFIGURATION_SERVICE_REDIS_HOST:0.0.0.0}
      port: ${CONFIGURATION_SERVICE_REDIS_PORT:6379}
      password: ${CONFIGURATION_SERVICE_REDIS_PASSWORD:}
  kafka:
    bootstrap-servers: ${CONFIGURATION_SERVICE_KAFKA_BOOTSTRAP_SERVERS:http://81.208.166.110:9092}

configuration-service:
  keycloak:
    base-url: ${CONFIGURATION_SERVICE_KEYCLOAK_BASE_URL:http://0.0.0.0:8181}
    issuer-uri: ${CONFIGURATION_SERVICE_KEYCLOAK_ISSUER_URI:http://0.0.0.0:8181/realms/platform-services}
    jwks-uri: ${CONFIGURATION_SERVICE_KEYCLOAK_JWKS_URI:http://0.0.0.0:8181/realms/platform-services/protocol/openid-connect/certs}
    admin-realm: ${CONFIGURATION_SERVICE_KEYCLOAK_ADMIN_REALM:master}
    admin-client-id: ${CONFIGURATION_SERVICE_KEYCLOAK_ADMIN_CLIENT_ID:}
    admin-client-secret: ${CONFIGURATION_SERVICE_KEYCLOAK_ADMIN_CLIENT_SECRET:}
    admin-username: ${CONFIGURATION_SERVICE_KEYCLOAK_ADMIN_USERNAME:}
    admin-password: ${CONFIGURATION_SERVICE_KEYCLOAK_ADMIN_PASSWORD:}
