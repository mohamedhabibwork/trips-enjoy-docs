# Intentionally empty — all Spring configuration lives in application.yml.
# This file is kept so the deployment manifests / Helm charts that
# inject configuration via the .properties file (or, historically, via
# Spring Cloud Config server) continue to work; the values here are
# overridden by the YAML profile files.
