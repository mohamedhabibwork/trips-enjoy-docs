spring:
  flyway:
    enabled: true
    schemas: payment
    default-schema: payment
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: payment

payment:
  security:
    enabled: false
  outbox:
    poll-interval-ms: 60000