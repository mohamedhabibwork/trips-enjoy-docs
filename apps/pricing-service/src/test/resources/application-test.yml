spring:
  flyway:
    enabled: true
    schemas: pricing
    default-schema: pricing
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: pricing

pricing:
  security:
    enabled: false
  outbox:
    poll-interval-ms: 60000