spring:
  flyway:
    enabled: true
    schemas: courier
    default-schema: courier
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: courier

courier:
  security:
    enabled: false
  outbox:
    poll-interval-ms: 60000