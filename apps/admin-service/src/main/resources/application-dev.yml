spring:
  datasource:
    url: ${ADMIN_SERVICE_DB_URL:jdbc:postgresql://0.0.0.0:5432/trips_enjoy?currentSchema=admin}
    username: ${ADMIN_SERVICE_DB_USERNAME:postgres}
    password: ${ADMIN_SERVICE_DB_PASSWORD:}
    hikari:
      connection-init-sql: 'CREATE SCHEMA IF NOT EXISTS admin;'
  data:
    redis:
      host: ${ADMIN_SERVICE_REDIS_HOST:0.0.0.0}
      port: ${ADMIN_SERVICE_REDIS_PORT:6379}
      password: ${ADMIN_SERVICE_REDIS_PASSWORD:}
  kafka:
    bootstrap-servers: ${ADMIN_SERVICE_KAFKA_BOOTSTRAP_SERVERS:http://81.208.166.110:9092}

admin-service:
  keycloak:
    base-url: ${ADMIN_SERVICE_KEYCLOAK_BASE_URL:http://0.0.0.0:8181}
    issuer-uri: ${ADMIN_SERVICE_KEYCLOAK_ISSUER_URI:http://0.0.0.0:8181/realms/platform-services}
    jwks-uri: ${ADMIN_SERVICE_KEYCLOAK_JWKS_URI:http://0.0.0.0:8181/realms/platform-services/protocol/openid-connect/certs}
    admin-realm: ${ADMIN_SERVICE_KEYCLOAK_ADMIN_REALM:master}
    admin-client-id: ${ADMIN_SERVICE_KEYCLOAK_ADMIN_CLIENT_ID:}
    admin-client-secret: ${ADMIN_SERVICE_KEYCLOAK_ADMIN_CLIENT_SECRET:}
    admin-username: ${ADMIN_SERVICE_KEYCLOAK_ADMIN_USERNAME:}
    admin-password: ${ADMIN_SERVICE_KEYCLOAK_ADMIN_PASSWORD:}
