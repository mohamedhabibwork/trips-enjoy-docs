spring:
  flyway:
    enabled: true
    schemas: admin
    default-schema: admin
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: admin

admin:
  security:
    enabled: false
  outbox:
    poll-interval-ms: 60000