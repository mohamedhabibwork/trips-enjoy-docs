package com.trips_enjoy.platform.autoconfigure

/**
 * Marker file imported via META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports.
 *
 * The actual auto-configurations live in each module's package and are
 * registered by the per-module imports SPI file. This marker class exists
 * to satisfy the umbrella gradle dependency and to give the autoconfigure
 * module compile-time artifacts to publish.
 */
internal class AutoConfiguration
